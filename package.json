{
  "name": "unemployment-calendar",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/validate-events.mjs && astro build",
    "preview": "astro preview",
    "check-events": "node scripts/validate-events.mjs"
  },
  "dependencies": {
    "astro": "^5.12.0"
  }
}
